#include <cmath>
#include <iomanip>
#include <iostream>
using namespace std;

int main()
{
    double a;
    cout << "Podaj wartoœæ a" << endl;
    cin >> a;
    
    double b;
    cout << "Podaj wartoœæ b" << endl;
    cin >> b;
    
    double c;
    cout << "Podaj wartoœæ c" << endl;
    cin >> c;
    
    double spr;
    if (a==0) cout << "A nie mo¿e byæ 0!!" << endl;
       else {
            
                double delta=b*b-4*a*c;
                cout << "delta: " << delta << endl;
                cin >> delta;
                
                double delta1=sqrt(delta);
    
            double d;
            
            if (d>0) {
                      
                     }
            else if (d==0) {
                 
                           }
            else cout << "BPR" << endl;

            
            }
    system("pause");
    return 0;
}
